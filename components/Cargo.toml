[package]
name = "components"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0.71"
features = "0.10.0"
once_cell = "1.18.0"
log = "0.4.22"
pretty_env_logger = "0.5.0"

[dev-dependencies]
tracing = { version = "0.1.40" }
rstest = "*"

[features]
debug = []
